// Shared constants for movement and colors.
export const MOVE_SPEED = 240;
export const JUMP_FORCE = 520;

// Use plain RGB tuples to avoid calling kaboom's rgb() before it's initialized
export const COLORS = {
	player: [80, 180, 255],
	coin: [255, 215, 0],
	platform: [220, 220, 220],
};

